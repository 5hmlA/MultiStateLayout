<resources>
    <style name="defauleTextStyle">
        <item name="android:textSize">15sp</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@android:color/holo_blue_bright</item>
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:buttonStyle">@style/defauleTextStyle</item>
        <item name="jmultistate">@style/multistate</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
        <!--<item name="loading">@layout/j_multitylayout_loading</item>-->
    <style name="multistate" parent="Jmultistate.style">
        <item name="error">@layout/j_multitylayout_error</item>
        <item name="loading">@layout/cust_loading2</item>
        <item name="empty">@layout/j_multitylayout_empty</item>
        <item name="state">loading</item>
    </style>

</resources>
